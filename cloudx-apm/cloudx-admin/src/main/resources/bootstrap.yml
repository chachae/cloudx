server:
  port: @apm.admin.port@

spring:
  application:
    name: @project.name@
  profiles:
    active: @profile.active@

  cloud:
    nacos:
      server-addr: @nacos.ip@

  security:
    user:
      name: @apm.admin.username@
      password: @apm.admin.password@

  boot:
    admin:
      ui:
        title: ${spring.application.name}

logging:
  level:
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error