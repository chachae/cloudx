spring:
  application:
    name: @project.name@
  profiles:
    active: @profile.active@

  cloud:
    nacos:
      config:
        server-addr: @nacos.ip@
        group: @nacos.group@
        prefix: @project.name@
        file-extension: yaml

logging:
  level:
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error